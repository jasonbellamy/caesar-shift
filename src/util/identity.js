/**
 * A function that returns the value of the parameter supplied to it
 *
 * @name identity
 * @function
 * @param {Mixed} x the value to return
 * @returns {Mixed} the value of the parameter
 */
export function identity(x) {
  return x;
}
