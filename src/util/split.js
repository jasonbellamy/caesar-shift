/**
 * Turn a string in to an array with each single char in each key
 *
 * @name split
 * @function
 * @param {String} x the seperator to split the string at
 * @param {String} y the string to split
 * @returns {Array} the new array
 */
export function split(x, y) {
  return y.split(x);
}
