/**
 * Check if the the provided ascii value is upper case
 *
 * @name isUpperCase
 * @function
 * @param {Number} x the unicode number
 * @returns {Boolean} is the provided value upper case
 */
export function isUpperCase(x) {
  return (x >= 65 && x <= 90);
}
