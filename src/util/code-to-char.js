/**
 * Converts a unicode value in to its equivalent string representation
 *
 * @name codeToChar
 * @function
 * @param {Number} x a unicode number
 * @returns {String} the string representation of the unicode number
 */
export function codeToChar(x) {
  return String.fromCharCode(x);
}
