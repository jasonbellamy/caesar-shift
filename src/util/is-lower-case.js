/**
 * Check if the the provided ascii value is lower case
 *
 * @name isLowerCase
 * @function
 * @param {Number} x the unicode number
 * @returns {Boolean} is the provided value lower case
 */
export function isLowerCase(x) {
  return (x >= 97 && x <= 122);
}
